name: weather_app
description: Project 2 - Weather app with API integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # TODO: Add these dependencies
  # dio: ^5.4.0  # HTTP client
  # shared_preferences: ^2.2.2  # For favorites

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
